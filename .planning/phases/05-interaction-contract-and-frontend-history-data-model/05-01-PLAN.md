---
phase: 05-interaction-contract-and-frontend-history-data-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/types/stocks.ts
  - web/src/data/mock-stocks.ts
autonomous: true
requirements:
  - FEH-01
must_haves:
  truths:
    - "HistoricalReportSummary contains exactly the fields needed for ledger list rows"
    - "HistoricalReportDetail extends HistoricalReportSummary with detail-panel fields"
    - "StockDetail uses HistoricalReportSummary[] for historicalReports and Record<string, HistoricalReportDetail> for historicalReportDetails"
    - "publishedAtMs is the only timestamp field on both types"
    - "localeHasFallback boolean exists on HistoricalReportSummary"
    - "Mock data compiles with the new type shapes"
  artifacts:
    - path: "web/src/types/stocks.ts"
      provides: "HistoricalReportSummary and HistoricalReportDetail interfaces, updated StockDetail"
      contains: "interface HistoricalReportSummary"
    - path: "web/src/data/mock-stocks.ts"
      provides: "Mock data split into summary arrays and detail lookup maps"
      contains: "HistoricalReportSummary"
  key_links:
    - from: "web/src/data/mock-stocks.ts"
      to: "web/src/types/stocks.ts"
      via: "import type { HistoricalReportSummary, HistoricalReportDetail }"
      pattern: "import.*HistoricalReportSummary.*HistoricalReportDetail"
---

<objective>
Define the stable summary/detail type contract and migrate mock data to the new shapes.

Purpose: Create the foundational type boundary that Phase 6 (backend) and Phase 7 (API integration) will target. The split separates list-row fields from detail-panel fields, matching the future API shape (GET /reports returns summaries, GET /reports/{id} returns detail).

Output: Updated type definitions in stocks.ts, migrated mock data in mock-stocks.ts with both summary arrays and detail lookup maps.
</objective>

<execution_context>
@/Users/huangchihan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huangchihan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-interaction-contract-and-frontend-history-data-model/05-CONTEXT.md
@.planning/phases/05-interaction-contract-and-frontend-history-data-model/05-RESEARCH.md

<interfaces>
<!-- Current type being split — executor must read this to understand the starting shape -->

From web/src/types/stocks.ts:
```typescript
export interface HistoricalReport {
  reportId: string
  publishedAtMs: number
  provenance: HistoricalReportProvenance
  valuationStatus: ValuationStatus
  thesisStatus: ThesisStatus
  technicalEntryStatus: TechnicalEntryStatus
  currentPrice: number
  bearFairValue: number
  baseFairValue: number
  bullFairValue: number
  summary: LocalizedText
  currentPriceImpliesBrief: LocalizedText
  currentPriceImplies: LocalizedText
  monitorNext: LocalizedText[]
  changeSummary: LocalizedText
  latest?: boolean
}

export interface StockDetail extends StockSummary {
  // ... existing fields ...
  historicalReports?: HistoricalReport[]
}
```

From web/src/i18n/types.ts:
```typescript
export type LocalizedText = string | Record<Locale, string>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define HistoricalReportSummary and HistoricalReportDetail types, update StockDetail</name>
  <files>web/src/types/stocks.ts</files>
  <read_first>
    - web/src/types/stocks.ts (current HistoricalReport interface, StockDetail interface)
    - web/src/i18n/types.ts (LocalizedText type definition)
    - .planning/phases/05-interaction-contract-and-frontend-history-data-model/05-CONTEXT.md (locked field lists)
  </read_first>
  <action>
Replace the `HistoricalReport` interface with two new interfaces and a transitional alias:

1. Add `HistoricalReportSummary` interface with these exact fields:
   - `reportId: string`
   - `publishedAtMs: number`
   - `provenance: HistoricalReportProvenance`
   - `valuationStatus: ValuationStatus`
   - `thesisStatus: ThesisStatus`
   - `technicalEntryStatus: TechnicalEntryStatus`
   - `currentPrice: number`
   - `bearFairValue: number`
   - `baseFairValue: number`
   - `bullFairValue: number`
   - `summary: LocalizedText`
   - `localeHasFallback: boolean`
   - `latest?: boolean`

2. Add `HistoricalReportDetail` that extends `HistoricalReportSummary` with:
   - `currentPriceImpliesBrief: LocalizedText`
   - `currentPriceImplies: LocalizedText`
   - `changeSummary: LocalizedText`
   - `monitorNext: LocalizedText[]`

3. Keep `HistoricalReport` as a transitional type alias:
   `export type HistoricalReport = HistoricalReportDetail`

4. Update `StockDetail` interface:
   - Change `historicalReports?: HistoricalReport[]` to `historicalReports?: HistoricalReportSummary[]`
   - Add `historicalReportDetails?: Record<string, HistoricalReportDetail>`

Place the new interfaces immediately before the `HistoricalReport` type alias. Keep the alias at the old location so existing imports continue to compile until Plan 02 updates them.
  </action>
  <verify>
    <automated>cd /Users/huangchihan/develop/deep-value/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "interface HistoricalReportSummary" web/src/types/stocks.ts returns 1
    - grep -c "interface HistoricalReportDetail extends HistoricalReportSummary" web/src/types/stocks.ts returns 1
    - grep -c "type HistoricalReport = HistoricalReportDetail" web/src/types/stocks.ts returns 1
    - grep "localeHasFallback: boolean" web/src/types/stocks.ts returns a match
    - grep "historicalReports?: HistoricalReportSummary\[\]" web/src/types/stocks.ts returns a match
    - grep "historicalReportDetails?: Record<string, HistoricalReportDetail>" web/src/types/stocks.ts returns a match
    - grep -c "publishedAtMs" web/src/types/stocks.ts returns exactly 1 (only one timestamp field across both interfaces, inherited via extends)
  </acceptance_criteria>
  <done>HistoricalReportSummary, HistoricalReportDetail, and updated StockDetail are defined in stocks.ts. The HistoricalReport alias preserves backward compatibility. TypeScript compilation passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate mock data to summary arrays and detail lookup maps</name>
  <files>web/src/data/mock-stocks.ts</files>
  <read_first>
    - web/src/data/mock-stocks.ts (current tsmHistoricalReports, adbeHistoricalReports, sofiHistoricalReports arrays and their usage in mockStocks)
    - web/src/types/stocks.ts (newly defined HistoricalReportSummary and HistoricalReportDetail)
    - .planning/phases/05-interaction-contract-and-frontend-history-data-model/05-CONTEXT.md (localeHasFallback computation rules)
  </read_first>
  <action>
Update mock-stocks.ts to use the new type shapes:

1. Change the import from `HistoricalReport` to `HistoricalReportDetail, HistoricalReportSummary` (from `../types/stocks`).

2. Add a utility function near the top of the file (after imports):
```typescript
function computeLocaleHasFallback(fields: LocalizedText[]): boolean {
  return fields.some((field) => typeof field === 'string')
}
```
This requires also importing `LocalizedText` from `../i18n/types`.

3. For TSM mock data:
   - Create `tsmHistoricalSummaries: HistoricalReportSummary[]` containing the 4 existing TSM revisions with only summary-level fields. Each entry must include `localeHasFallback: false` (all TSM fields are `Record<Locale, string>`). Mark the last entry (tsm-20260316-full-review) with `latest: true`.
   - Create `tsmHistoricalDetails: Record<string, HistoricalReportDetail>` keyed by reportId. Each value spreads the corresponding summary and adds `currentPriceImpliesBrief`, `currentPriceImplies`, `changeSummary`, `monitorNext` from the existing mock data.

4. For ADBE mock data:
   - Create `adbeHistoricalSummaries: HistoricalReportSummary[]` with the single ADBE revision. Set `localeHasFallback: true` because ADBE uses plain string fields (not Record<Locale, string>). Mark it with `latest: true`.
   - Create `adbeHistoricalDetails: Record<string, HistoricalReportDetail>` with the single ADBE entry.

5. For SOFI mock data:
   - Create `sofiHistoricalSummaries: HistoricalReportSummary[] = []`
   - Create `sofiHistoricalDetails: Record<string, HistoricalReportDetail> = {}`

6. Update the TSM entry in `mockStocks` array:
   - Change `historicalReports: tsmHistoricalReports` to `historicalReports: tsmHistoricalSummaries`
   - Add `historicalReportDetails: tsmHistoricalDetails`

7. Update the ADBE entry in `mockStocks` array:
   - Change `historicalReports: adbeHistoricalReports` to `historicalReports: adbeHistoricalSummaries`
   - Add `historicalReportDetails: adbeHistoricalDetails`

8. Update the SOFI entry in `mockStocks` array:
   - Change `historicalReports: sofiHistoricalReports` to `historicalReports: sofiHistoricalSummaries`
   - Add `historicalReportDetails: sofiHistoricalDetails`

9. Remove or comment out the old `tsmHistoricalReports`, `adbeHistoricalReports`, `sofiHistoricalReports` variables after migration.

Use `computeLocaleHasFallback` when constructing summaries to validate the manual boolean — e.g. assert that TSM returns false and ADBE returns true. In practice, just set the booleans directly and use the utility as documentation of intent (it will be reused in Phase 7).
  </action>
  <verify>
    <automated>cd /Users/huangchihan/develop/deep-value/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "HistoricalReportSummary" web/src/data/mock-stocks.ts returns matches (used in type annotations)
    - grep "HistoricalReportDetail" web/src/data/mock-stocks.ts returns matches (used in type annotations)
    - grep "tsmHistoricalSummaries" web/src/data/mock-stocks.ts returns a match
    - grep "tsmHistoricalDetails" web/src/data/mock-stocks.ts returns a match
    - grep "adbeHistoricalSummaries" web/src/data/mock-stocks.ts returns a match
    - grep "adbeHistoricalDetails" web/src/data/mock-stocks.ts returns a match
    - grep "localeHasFallback: true" web/src/data/mock-stocks.ts returns at least 1 match (ADBE)
    - grep "localeHasFallback: false" web/src/data/mock-stocks.ts returns at least 1 match (TSM)
    - grep "historicalReportDetails:" web/src/data/mock-stocks.ts returns at least 2 matches (TSM and ADBE entries in mockStocks)
    - grep "computeLocaleHasFallback" web/src/data/mock-stocks.ts returns a match
    - TypeScript compilation passes with zero errors
  </acceptance_criteria>
  <done>Mock data is split into summary arrays and detail lookup maps. TSM has localeHasFallback: false, ADBE has localeHasFallback: true. The mockStocks array uses the new field names. TypeScript compilation passes.</done>
</task>

</tasks>

<verification>
- `cd web && pnpm build` completes with zero errors
- `grep "HistoricalReportSummary" web/src/types/stocks.ts` confirms new summary type exists
- `grep "HistoricalReportDetail extends HistoricalReportSummary" web/src/types/stocks.ts` confirms extension relationship
- `grep "historicalReportDetails" web/src/data/mock-stocks.ts` confirms detail lookup maps exist
</verification>

<success_criteria>
- HistoricalReportSummary and HistoricalReportDetail are exported from stocks.ts
- StockDetail has both historicalReports (summary) and historicalReportDetails (detail lookup) fields
- Mock data compiles against the new types
- localeHasFallback is set correctly: false for TSM (bilingual), true for ADBE (English-only)
- publishedAtMs is the only timestamp field on the types
- pnpm build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-interaction-contract-and-frontend-history-data-model/05-01-SUMMARY.md`
</output>
