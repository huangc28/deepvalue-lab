---
phase: 01-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/skills/deepvalue-stock-analysis/SKILL.md
  - docs/analysis/deepvalue-lab-report-contract.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02

must_haves:
  truths:
    - "SKILL.md states that every analysis run must produce a zh-TW markdown report alongside the English one"
    - "SKILL.md specifies the archive naming convention: <TICKER>-analysis-zh-TW.md"
    - "Report contract specifies zh-TW translation rules: plain-language leads before technical detail, acronyms explained on first use, financial jargon retained"
    - "Report contract includes a zh-TW section skeleton matching the 15-section English structure"
  artifacts:
    - path: ".agents/skills/deepvalue-stock-analysis/SKILL.md"
      provides: "Updated skill with zh-TW output requirements and archive naming"
      contains: "zh-TW"
    - path: "docs/analysis/deepvalue-lab-report-contract.md"
      provides: "zh-TW translation rules and simplification guidelines"
      contains: "zh-TW"
  key_links:
    - from: ".agents/skills/deepvalue-stock-analysis/SKILL.md"
      to: "docs/analysis/deepvalue-lab-report-contract.md"
      via: "SKILL.md references the report contract as the authoritative structure for zh-TW output"
      pattern: "report-contract|report contract"
---

<objective>
Update the skill output contract (SKILL.md) and the report contract to fully specify zh-TW requirements: what the skill must produce, archive naming, and zh-TW translation rules.

Purpose: Phase 2 implementation depends on these contracts being unambiguous. Executors in Phase 2 will read these docs to understand exactly what zh-TW output must look like.
Output: SKILL.md with zh-TW output section, report contract with zh-TW translation rules block.
</objective>

<execution_context>
@/Users/huangchihan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huangchihan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add zh-TW output requirements to SKILL.md</name>
  <files>.agents/skills/deepvalue-stock-analysis/SKILL.md</files>

  <read_first>
    - .agents/skills/deepvalue-stock-analysis/SKILL.md (the file being modified — read before touching)
    - docs/analysis/deepvalue-lab-report-contract.md (15-section structure this must reference)
    - .planning/REQUIREMENTS.md (DOC-01 definition and constraints)
  </read_first>

  <action>
In SKILL.md, locate the "Save To Research Archive" subsection (currently under "Structured Data Generation"). Extend this subsection and the surrounding output instructions to specify zh-TW requirements.

Make these exact additions:

1. In "Save To Research Archive", after the two existing numbered steps, add:

```
3. Write the zh-TW markdown report to `research/archive/YYYY/MM/DD/<TICKER>-analysis-zh-TW.md`.
```

2. Add a new subsection immediately after "Save To Research Archive" titled **"zh-TW Report Output"** with the following content:

```markdown
### zh-TW Report Output

Every analysis run must produce a zh-TW markdown report alongside the English report.

The zh-TW report:
- follows the same 15-section structure defined in the report contract
- is saved as `research/archive/YYYY/MM/DD/<TICKER>-analysis-zh-TW.md`
- uses lightly simplified language (see report contract for zh-TW translation rules)
- retains unavoidable financial jargon — jargon education is a separate future feature

The zh-TW report is mandatory. An analysis run that produces an English report without a zh-TW report is incomplete.
```

3. In the "Completion Check" section, in the list of "Also verify that:" items, add:

```
- the zh-TW markdown report was produced and saved to `research/archive/YYYY/MM/DD/<TICKER>-analysis-zh-TW.md`
```

Do not remove or reorder existing content. Add only the items above at the specified locations.
  </action>

  <verify>
    <automated>grep -n "zh-TW" /Users/huangchihan/develop/profound-stock/.agents/skills/deepvalue-stock-analysis/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - SKILL.md contains the string "zh-TW Report Output" (the new subsection heading)
    - SKILL.md contains "analysis-zh-TW.md" (archive naming convention)
    - SKILL.md contains "An analysis run that produces an English report without a zh-TW report is incomplete"
    - SKILL.md contains "zh-TW markdown report was produced and saved" in the Completion Check section
    - The existing 15-section output contract list in SKILL.md remains unchanged (grep for "## 9. Bull Case" still returns a match in the file — the section numbering in SKILL.md uses its own inline list, so verify the word "Bull Case" still appears)
  </acceptance_criteria>

  <done>SKILL.md specifies that every analysis produces a zh-TW report, names the archive file convention, and includes zh-TW production in the completion check.</done>
</task>

<task type="auto">
  <name>Task 2: Add zh-TW translation rules to the report contract</name>
  <files>docs/analysis/deepvalue-lab-report-contract.md</files>

  <read_first>
    - docs/analysis/deepvalue-lab-report-contract.md (the file being modified — read before touching)
    - .planning/REQUIREMENTS.md (DOC-02 definition: plain-language leads, acronyms explained on first use, financial jargon retained)
    - .planning/PROJECT.md (constraints: light simplification, same 15-section structure, audience includes finance-literate readers)
  </read_first>

  <action>
In `docs/analysis/deepvalue-lab-report-contract.md`, add a new section after the existing "## 8. Minimal Output Skeleton" section and before "## 9. Archive And Writeback Rule".

Insert the following as a new section:

```markdown
## 8.5 zh-TW Report Contract

### Purpose

Every analysis run produces a Traditional Chinese (繁體中文) report alongside the English report. This section defines the translation and simplification rules for the zh-TW report.

### Structure Rule

The zh-TW report must follow the same 15-section structure as the English report, in the same order. No sections may be added, removed, or reordered.

Use the same section skeleton as Section 8, with all headings translated to Traditional Chinese. Example heading translations:

| English | zh-TW |
|---------|-------|
| Business Classification | 業務分類 |
| Thesis | 投資論點 |
| Variant Perception | 差異認知 |
| Valuation Lens | 估值方法 |
| Current Valuation Snapshot | 當前估值快照 |
| Recent News And News-To-Model Translation | 近期新聞與模型影響 |
| Bear Case | 悲觀情景 |
| Base Case | 基準情景 |
| Bull Case | 樂觀情景 |
| What The Current Price Implies | 當前股價隱含的預期 |
| Provisional Conclusion | 初步結論 |
| Thesis Status | 論點狀態 |
| Technical Entry Status | 技術入場狀態 |
| What To Monitor Next | 後續監察重點 |
| Sources Used | 資料來源 |

### Translation Rules

Apply these rules when writing the zh-TW report:

1. **Plain-language lead**: Every section opens with one or two plain-language sentences that state the key point in clear, accessible terms before any technical detail. This lead should be readable by a finance-literate reader who is not a specialist.

2. **Acronyms explained on first use**: When an acronym (e.g., EPS, FCF, EBITDA, RSI, EMA, P/E) appears for the first time in the zh-TW report, spell it out in full in Traditional Chinese followed by the acronym in parentheses. Subsequent uses may use the acronym alone.
   - Example: 每股盈利（EPS）/ 自由現金流（FCF）/ 息稅折舊攤銷前利潤（EBITDA）

3. **Financial jargon retained**: Unavoidable financial jargon (valuation multiples, accounting terms, technical analysis terms) is retained in the zh-TW report. Do not replace jargon with simplified terms. Jargon education is a separate future feature.

4. **Simplification level**: Light. Write clearer sentences and use shorter constructions where possible. Do not fundamentally rewrite the analytical content. The zh-TW report is a translation with light simplification, not a summary or a different analysis.

5. **Numbers and data**: Retain all numerical data, fair values, multiples, and price targets unchanged from the English report. Do not round, adjust, or omit quantitative data.

6. **Judgment and tone**: Preserve the buy-side memo tone. Judgments, thesis status labels (intact / watch / broken), and technical entry labels (favorable / neutral / stretched) carry the same meaning in the zh-TW report.

### Archive Naming

The zh-TW report is saved as:

```
research/archive/YYYY/MM/DD/<TICKER>-analysis-zh-TW.md
```

This file exists alongside the English report (`<TICKER>-analysis.md`) in the same directory.

### Completion Rule

A zh-TW report is incomplete if:

- any of the 15 required sections is missing
- any section is present in English only (all body content must be in Traditional Chinese)
- any numerical data from the English report is omitted
- acronyms are not explained on first use
```

Do not modify any existing sections. Insert only the block above between sections 8 and 9.
  </action>

  <verify>
    <automated>grep -n "zh-TW" /Users/huangchihan/develop/profound-stock/docs/analysis/deepvalue-lab-report-contract.md</automated>
  </verify>

  <acceptance_criteria>
    - `docs/analysis/deepvalue-lab-report-contract.md` contains the heading "## 8.5 zh-TW Report Contract"
    - File contains "Plain-language lead" (translation rule 1)
    - File contains "Acronyms explained on first use" (translation rule 2)
    - File contains "Financial jargon retained" (translation rule 3)
    - File contains "analysis-zh-TW.md" (archive naming convention)
    - File contains "業務分類" (Traditional Chinese heading for Business Classification — confirms TC content present)
    - File contains "A zh-TW report is incomplete if" (completion rule present)
    - The original "## 9. Archive And Writeback Rule" section still exists in the file (existing content not removed)
    - The original "## 8. Minimal Output Skeleton" section still exists in the file
  </acceptance_criteria>

  <done>Report contract includes a dedicated zh-TW section that specifies: same 15-section structure, plain-language leads, acronyms explained on first use, jargon retained, light simplification, archive naming, and a completion rule.</done>
</task>

</tasks>

<verification>
Run both acceptance criteria checks manually after execution:

1. `grep -c "zh-TW" .agents/skills/deepvalue-stock-analysis/SKILL.md` — should return > 0
2. `grep "zh-TW Report Output" .agents/skills/deepvalue-stock-analysis/SKILL.md` — should return the heading line
3. `grep "analysis-zh-TW.md" .agents/skills/deepvalue-stock-analysis/SKILL.md` — should return the archive path
4. `grep "8.5 zh-TW Report Contract" docs/analysis/deepvalue-lab-report-contract.md` — should return the heading
5. `grep "Plain-language lead" docs/analysis/deepvalue-lab-report-contract.md` — should return rule 1
6. `grep "## 9. Archive And Writeback Rule" docs/analysis/deepvalue-lab-report-contract.md` — confirms existing content preserved
</verification>

<success_criteria>
- SKILL.md: every analysis run is required to produce a zh-TW report; archive naming convention `<TICKER>-analysis-zh-TW.md` is stated; zh-TW production appears in the completion check
- Report contract: section 8.5 exists with plain-language lead rule, acronym-first-use rule, jargon-retained rule, light simplification rule, archive naming convention, and a completion rule; all 15 section headings listed with Traditional Chinese translations
</success_criteria>

<output>
After completion, create `.planning/phases/01-contracts/01-01-SUMMARY.md` using the summary template.
</output>
