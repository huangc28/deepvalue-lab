---
phase: 01-contracts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/analysis/deepvalue-lab-agent-execution-sop.md
autonomous: true
requirements:
  - DOC-03

must_haves:
  truths:
    - "Agent execution SOP contains an explicit zh-TW translation step in the numbered workflow sequence"
    - "The translation step appears after the English report is written and before the completion check"
    - "SOP states the zh-TW report is mandatory, not optional"
  artifacts:
    - path: "docs/analysis/deepvalue-lab-agent-execution-sop.md"
      provides: "SOP with zh-TW translation step integrated into the workflow"
      contains: "zh-TW"
  key_links:
    - from: "docs/analysis/deepvalue-lab-agent-execution-sop.md"
      to: "docs/analysis/deepvalue-lab-report-contract.md"
      via: "SOP translation step references the report contract for zh-TW translation rules"
      pattern: "report contract|report-contract"
---

<objective>
Update the agent execution SOP to add an explicit zh-TW translation step in the numbered workflow sequence, so agents know they must produce a zh-TW report as part of every analysis run.

Purpose: Without this, an agent following the SOP step-by-step would produce only an English report. Adding a translation step makes zh-TW output a mandatory workflow obligation, not an optional extra.
Output: SOP with a new numbered step for zh-TW translation inserted after the English report step and before the completion check.
</objective>

<execution_context>
@/Users/huangchihan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huangchihan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert zh-TW translation step into the SOP workflow</name>
  <files>docs/analysis/deepvalue-lab-agent-execution-sop.md</files>

  <read_first>
    - docs/analysis/deepvalue-lab-agent-execution-sop.md (the file being modified — read in full before touching)
    - docs/analysis/deepvalue-lab-report-contract.md (the zh-TW translation rules the new step must reference)
    - .planning/REQUIREMENTS.md (DOC-03 definition: translation step must appear in the workflow sequence)
  </read_first>

  <action>
In `docs/analysis/deepvalue-lab-agent-execution-sop.md`, make the following targeted modifications:

**Modification 1 — Renumber the workflow steps.**

The current workflow has Steps 1–13. Insert a new step after the current "Step 11: Write The Report Using The Contract" and before the current "Step 12: Perform A Completion Check". This new step becomes Step 12; shift the existing Steps 12 and 13 to become Steps 13 and 14.

**Modification 2 — Add the new Step 12 section.**

Insert the following block immediately after the "### Step 11: Write The Report Using The Contract" section and before the (now renumbered) "### Step 13: Perform A Completion Check" section:

```markdown
### Step 12: Produce The zh-TW Report

After writing the English report, produce a Traditional Chinese (繁體中文) version of the report.

The zh-TW report is mandatory. An analysis that produces only an English report is incomplete.

Requirements for the zh-TW report:

- Follow the same 15-section structure as the English report, in the same order
- Open every section with a plain-language lead sentence before technical detail
- Explain every acronym on first use (e.g., 每股盈利（EPS）/ 自由現金流（FCF）)
- Retain unavoidable financial jargon — do not replace it with simplified terms
- Keep all numerical data, fair values, multiples, and price targets identical to the English report
- Apply light simplification: clearer sentences and shorter constructions where possible, but do not rewrite the analytical content

For the full set of zh-TW translation rules, see the report contract:
`docs/analysis/deepvalue-lab-report-contract.md` — Section 8.5 zh-TW Report Contract

Save the zh-TW report to:
`research/archive/YYYY/MM/DD/<TICKER>-analysis-zh-TW.md`

This file exists alongside the English report (`<TICKER>-analysis.md`) in the same directory.
```

**Modification 3 — Update the failure conditions.**

In "## 7. Failure Conditions", add the following item to the list:

```
- produced only an English report without a zh-TW report
```

**Modification 4 — Update the SOP summary.**

In "## 8. SOP Summary", update the numbered list. The current list has 7 items. Expand it so zh-TW translation is explicit. Replace the current list:

Current:
```
1. method from second brain
2. facts from current sources
3. valuation and scenario reasoning
4. technicals as timing support
5. structured report
6. completion check
7. benchmark quality check
```

Replace with:
```
1. method from second brain
2. facts from current sources
3. valuation and scenario reasoning
4. technicals as timing support
5. structured report (English)
6. zh-TW report (same 15-section structure, light simplification)
7. completion check
8. benchmark quality check
```

Do not modify any other sections. Make only the four modifications listed above.
  </action>

  <verify>
    <automated>grep -n "zh-TW\|Step 12\|Step 13\|Step 14" /Users/huangchihan/develop/profound-stock/docs/analysis/deepvalue-lab-agent-execution-sop.md</automated>
  </verify>

  <acceptance_criteria>
    - File contains "### Step 12: Produce The zh-TW Report" (new step heading)
    - File contains "### Step 13: Perform A Completion Check" (old Step 12 renumbered)
    - File contains "### Step 14: Perform A Benchmark Quality Check" (old Step 13 renumbered)
    - File contains "The zh-TW report is mandatory. An analysis that produces only an English report is incomplete."
    - File contains "analysis-zh-TW.md" (archive naming in the new step)
    - File contains "Plain-language lead" OR "plain-language lead" in the new step body
    - File contains "produced only an English report without a zh-TW report" (in Failure Conditions)
    - File contains "zh-TW report (same 15-section structure, light simplification)" (in SOP Summary)
    - File still contains "## 2. Core Rule" (existing section preserved)
    - File still contains "## 4. Output Rules" (existing section preserved)
  </acceptance_criteria>

  <done>Agent execution SOP has a numbered Step 12 for zh-TW report production, the failure conditions list includes producing only English, and the SOP summary reflects the zh-TW step.</done>
</task>

</tasks>

<verification>
Run these checks after execution:

1. `grep "Step 12: Produce" docs/analysis/deepvalue-lab-agent-execution-sop.md` — must return the new heading
2. `grep "Step 13: Perform A Completion" docs/analysis/deepvalue-lab-agent-execution-sop.md` — must return renumbered old Step 12
3. `grep "zh-TW report is mandatory" docs/analysis/deepvalue-lab-agent-execution-sop.md` — must return the mandatory statement
4. `grep "produced only an English report" docs/analysis/deepvalue-lab-agent-execution-sop.md` — must return failure condition
5. `grep -c "Step" docs/analysis/deepvalue-lab-agent-execution-sop.md` — should return more lines than before (new step added)
</verification>

<success_criteria>
- SOP Step 12 exists and is titled "Produce The zh-TW Report"
- Steps 12 and 13 (completion check, benchmark quality check) are renumbered to 13 and 14
- The translation step specifies: plain-language leads, acronym-first-use, jargon retained, numerical data unchanged, archive path
- Failure conditions include producing English-only output
- SOP summary lists zh-TW report as a named step in the sequence
</success_criteria>

<output>
After completion, create `.planning/phases/01-contracts/01-02-SUMMARY.md` using the summary template.
</output>
