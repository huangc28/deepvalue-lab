---
phase: 03-add-zh-tw-stockdetail-json-support-to-backend-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - be/turso/schema.sql
  - be/turso/query/stocks.sql
  - be/turso/migrations/*published_stock_details_zh_tw_locale.sql
  - be/lib/app/turso_models/models.go
  - be/lib/app/turso_models/stocks.sql.go
  - be/lib/pkg/r2/client.go
  - be/lib/app/stocks/publish_handler.go
  - be/lib/app/stocks/publish_handler_test.go
autonomous: true
requirements:
  - API-01
  - PH3-STORAGE
  - PH3-PUBLISH

must_haves:
  truths:
    - "published_stock_details has zh-TW storage columns `r2_detail_zh_tw_key` and `summary_json_zh_tw` with safe defaults and valid-json constraint"
    - "The publish request accepts optional `stockDetailZhTW` JSON without breaking existing EN-only callers"
    - "When `stockDetailZhTW` is provided, the backend uploads a zh-TW detail artifact to an R2 key ending in `.zh-TW.json`, extracts a zh-TW summary, and persists both zh-TW columns"
    - "When `stockDetailZhTW` is absent, publish behavior remains backward compatible and zh-TW columns stay at default values"
    - "The publish response includes `r2DetailZhTWKey` only for bilingual publishes or otherwise leaves it empty without failing the request"
  artifacts:
    - path: "be/turso/schema.sql"
      provides: "Canonical schema with zh-TW detail and summary columns"
      contains: "r2_detail_zh_tw_key"
    - path: "be/lib/app/stocks/publish_handler.go"
      provides: "Bilingual publish flow for EN + optional zh-TW StockDetail payloads"
      contains: "stockDetailZhTW"
    - path: "be/lib/pkg/r2/client.go"
      provides: "Locale-specific zh-TW detail key helper"
      contains: ".zh-TW.json"
  key_links:
    - from: "be/lib/app/stocks/publish_handler.go"
      to: "be/turso/query/stocks.sql"
      via: "UpsertPublishedStockDetail persists EN and zh-TW summary/detail keys together"
      pattern: "SummaryJsonZhTw|R2DetailZhTwKey|summary_json_zh_tw|r2_detail_zh_tw_key"
    - from: "be/lib/pkg/r2/client.go"
      to: "be/lib/app/stocks/publish_handler.go"
      via: "publish handler uses helper-generated zh-TW detail keys instead of string concatenation"
      pattern: "zh-TW.json"
---

<objective>
Extend backend persistence and publish behavior so the stock publish endpoint can optionally accept zh-TW `StockDetail` JSON, store zh-TW summary/detail pointers alongside the existing EN data, and upload locale-specific detail artifacts to R2 without breaking current EN-only callers.

Purpose: Phase 2 already produces zh-TW StockDetail artifacts. The backend now needs a durable place to persist them and a publish contract that can accept them.
Output: schema/query/sqlc surfaces updated, publish handler supports `stockDetailZhTW`, and publish-path tests cover both EN-only and bilingual requests.
</objective>

<execution_context>
@/Users/huangchihan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangchihan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-add-zh-tw-stockdetail-json-support-to-backend-api/03-CONTEXT.md
@.planning/phases/03-add-zh-tw-stockdetail-json-support-to-backend-api/03-RESEARCH.md
@.planning/phases/03-add-zh-tw-stockdetail-json-support-to-backend-api/03-VALIDATION.md

<interfaces>
Persist these exact zh-TW additions in `published_stock_details`:

- `r2_detail_zh_tw_key TEXT NOT NULL DEFAULT ''`
- `summary_json_zh_tw TEXT NOT NULL DEFAULT '{}' CHECK (json_valid(summary_json_zh_tw))`

Publish request contract after this plan:

```json
{
  "report": {
    "markdown": "...",
    "provenance": "..."
  },
  "stockDetail": { "...": "EN detail JSON" },
  "stockDetailZhTW": { "...": "zh-TW detail JSON" }
}
```

`stockDetailZhTW` is optional. When present, the response must expose `r2DetailZhTWKey`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add zh-TW persistence columns and regenerate sqlc models</name>
  <files>be/turso/schema.sql, be/turso/query/stocks.sql, be/turso/migrations/*published_stock_details_zh_tw_locale.sql, be/lib/app/turso_models/models.go, be/lib/app/turso_models/stocks.sql.go</files>

  <read_first>
    - `be/turso/schema.sql` — current canonical schema; read the full `published_stock_details` table before editing
    - `be/turso/query/stocks.sql` — current `UpsertPublishedStockDetail`, `GetPublishedStockDetail`, and `ListPublishedStockDetails` queries
    - `be/turso/migrations/20260318100726_stock_detail_to_r2.sql` — reference replacement-table migration pattern and trigger recreation
    - `be/sqlc.yaml` — confirm sqlc input/output paths before regenerating generated files
    - `be/lib/app/turso_models/models.go` — current generated `PublishedStockDetail` struct shape
    - `be/lib/app/turso_models/stocks.sql.go` — current generated `UpsertPublishedStockDetailParams` and scan order
  </read_first>

  <action>
Create a new goose migration using suffix `published_stock_details_zh_tw_locale.sql` under `be/turso/migrations/`. In the migration `Up` section, rebuild `published_stock_details` as `published_stock_details_new` with these exact columns, in this order:

1. `ticker TEXT PRIMARY KEY`
2. `report_id TEXT NOT NULL`
3. `r2_report_key TEXT NOT NULL DEFAULT ''`
4. `r2_detail_key TEXT NOT NULL DEFAULT ''`
5. `r2_detail_zh_tw_key TEXT NOT NULL DEFAULT ''`
6. `summary_json TEXT NOT NULL DEFAULT '{}' CHECK (json_valid(summary_json))`
7. `summary_json_zh_tw TEXT NOT NULL DEFAULT '{}' CHECK (json_valid(summary_json_zh_tw))`
8. `published_at_ms INTEGER NOT NULL DEFAULT (unixepoch('now') * 1000)`
9. `updated_at_ms INTEGER NOT NULL DEFAULT (unixepoch('now') * 1000)`

Copy existing rows into the new table with:

- `r2_detail_zh_tw_key` set to `''`
- `summary_json_zh_tw` set to `'{}'`

Then drop the old table, rename the new table back to `published_stock_details`, and recreate `trg_published_stock_details_touch_updated_at`.

In the migration `Down` section, rebuild the table back to the pre-zh-TW column set (`ticker`, `report_id`, `r2_report_key`, `r2_detail_key`, `summary_json`, `published_at_ms`, `updated_at_ms`) and restore the same trigger.

Update `be/turso/schema.sql` to match the post-migration shape exactly.

Update `be/turso/query/stocks.sql` so `UpsertPublishedStockDetail` inserts and updates both zh-TW columns:

- insert columns: `(ticker, report_id, r2_report_key, r2_detail_key, r2_detail_zh_tw_key, summary_json, summary_json_zh_tw, published_at_ms)`
- on conflict update fields: `report_id`, `r2_report_key`, `r2_detail_key`, `r2_detail_zh_tw_key`, `summary_json`, `summary_json_zh_tw`, `published_at_ms`, `updated_at_ms`

Leave `GetPublishedStockDetail` and `ListPublishedStockDetails` as `SELECT *`.

Run `cd be && make gen/sqlc` so generated files reflect the new schema/query shape.
  </action>

  <verify>
    <automated>cd /Users/huangchihan/develop/profound-stock/be && go test ./...</automated>
  </verify>

  <acceptance_criteria>
    - `grep "r2_detail_zh_tw_key" /Users/huangchihan/develop/profound-stock/be/turso/schema.sql` returns a match
    - `grep "summary_json_zh_tw" /Users/huangchihan/develop/profound-stock/be/turso/schema.sql` returns a match
    - `find /Users/huangchihan/develop/profound-stock/be/turso/migrations -name '*published_stock_details_zh_tw_locale.sql'` returns exactly one file
    - `grep "r2_detail_zh_tw_key" /Users/huangchihan/develop/profound-stock/be/turso/query/stocks.sql` returns a match in `UpsertPublishedStockDetail`
    - `grep "summary_json_zh_tw" /Users/huangchihan/develop/profound-stock/be/turso/query/stocks.sql` returns a match in `UpsertPublishedStockDetail`
    - `grep "R2DetailZhTwKey" /Users/huangchihan/develop/profound-stock/be/lib/app/turso_models/models.go` returns a match
    - `grep "SummaryJsonZhTw" /Users/huangchihan/develop/profound-stock/be/lib/app/turso_models/stocks.sql.go` returns a match
    - `cd /Users/huangchihan/develop/profound-stock/be && go test ./...` exits 0
  </acceptance_criteria>

  <done>The backend schema, sqlc query surface, and generated models all understand zh-TW summary/detail storage alongside the existing EN columns.</done>
</task>

<task type="auto">
  <name>Task 2: Extend publish flow to accept optional zh-TW StockDetail payloads and persist zh-TW artifacts</name>
  <files>be/lib/pkg/r2/client.go, be/lib/app/stocks/publish_handler.go, be/lib/app/stocks/publish_handler_test.go</files>

  <read_first>
    - `be/lib/pkg/r2/client.go` — existing `DetailKey()` and upload/download helpers
    - `be/lib/app/stocks/publish_handler.go` — read the full publish flow, especially request decoding, validation, upload order, summary extraction, and response payload
    - `be/lib/app/turso_models/stocks.sql.go` — regenerated `UpsertPublishedStockDetailParams` field names and order after Task 1
    - `be/turso/query/stocks.sql` — confirm the exact zh-TW columns being persisted
    - `be/lib/router/core.go` — reference `Handler` contract if you need local testing helpers
  </read_first>

  <action>
In `be/lib/pkg/r2/client.go`, add a locale-specific detail-key helper that returns `reports/{ticker}/{YYYYMMDD}/{reportID}.zh-TW.json`. Keep the existing EN `DetailKey()` unchanged. Name the new helper something explicit, for example `DetailKeyZhTW`, and keep the suffix logic inside this package.

In `be/lib/app/stocks/publish_handler.go`, make these exact contract changes:

1. Extend `publishRequest` with optional field:
   - `StockDetailZhTW json.RawMessage \`json:"stockDetailZhTW"\``

2. Keep EN validation exactly as today for `stockDetail`.

3. When `stockDetailZhTW` is present and non-empty:
   - upload the raw JSON bytes to the zh-TW R2 detail key from the new helper,
   - unmarshal it into the existing `summaryFields` struct,
   - marshal the zh-TW summary back to JSON,
   - set `R2DetailZhTwKey` and `SummaryJsonZhTw` in `UpsertPublishedStockDetailParams`.

4. When `stockDetailZhTW` is absent or empty:
   - do not upload any zh-TW artifact,
   - pass `R2DetailZhTwKey: ""` and `SummaryJsonZhTw: "{}"` to `UpsertPublishedStockDetail`.

5. Preserve the existing EN upload order and auto-subscribe behavior.

6. Extend the success response map with key `r2DetailZhTWKey`.

7. On zh-TW upload or zh-TW summary extraction failure, return HTTP 500 and a concrete message mentioning zh-TW detail handling, for example:
   - `failed to upload zh-TW detail artifact`
   - `failed to extract zh-TW summary`

Add `be/lib/app/stocks/publish_handler_test.go` with package-local tests that cover:

- EN-only request keeps `r2DetailZhTWKey` empty and persists default zh-TW columns.
- bilingual request persists zh-TW key/summary and returns `r2DetailZhTWKey`.
- invalid `stockDetailZhTW` JSON triggers `422` before persistence only if the field is provided and malformed.

If the concrete `*turso_models.Queries` dependency makes tests brittle, add minimal package-local interfaces in `publish_handler.go` for only the methods `Handle` uses, then wire the constructor to accept that interface while still passing `*turso_models.Queries` from production code.
  </action>

  <verify>
    <automated>cd /Users/huangchihan/develop/profound-stock/be && go test ./...</automated>
  </verify>

  <acceptance_criteria>
    - `grep 'stockDetailZhTW' /Users/huangchihan/develop/profound-stock/be/lib/app/stocks/publish_handler.go` returns a match
    - `grep 'r2DetailZhTWKey' /Users/huangchihan/develop/profound-stock/be/lib/app/stocks/publish_handler.go` returns a match in the success response
    - `grep 'failed to upload zh-TW detail artifact' /Users/huangchihan/develop/profound-stock/be/lib/app/stocks/publish_handler.go` returns a match
    - `grep 'failed to extract zh-TW summary' /Users/huangchihan/develop/profound-stock/be/lib/app/stocks/publish_handler.go` returns a match
    - `grep '\\.zh-TW\\.json' /Users/huangchihan/develop/profound-stock/be/lib/pkg/r2/client.go` returns a match
    - `/Users/huangchihan/develop/profound-stock/be/lib/app/stocks/publish_handler_test.go` exists
    - `cd /Users/huangchihan/develop/profound-stock/be && go test ./...` exits 0
  </acceptance_criteria>

  <done>The publish endpoint can accept optional zh-TW StockDetail JSON, persist locale-specific detail/summary data, and prove backward compatibility through automated tests.</done>
</task>

</tasks>

<verification>
Run these checks after execution:

1. `grep "r2_detail_zh_tw_key" be/turso/schema.sql`
2. `grep "summary_json_zh_tw" be/turso/query/stocks.sql`
3. `find be/turso/migrations -name '*published_stock_details_zh_tw_locale.sql'`
4. `grep "stockDetailZhTW" be/lib/app/stocks/publish_handler.go`
5. `grep "r2DetailZhTWKey" be/lib/app/stocks/publish_handler.go`
6. `cd be && go test ./...`
</verification>

<success_criteria>
- backend persistence layer stores optional zh-TW detail and summary data beside EN data
- publish handler remains compatible with current EN-only clients while supporting bilingual publish payloads
- locale-specific R2 detail key helper exists and is used by the publish path
- automated tests prove both EN-only and bilingual publish flows
</success_criteria>

<output>
After completion, create `.planning/phases/03-add-zh-tw-stockdetail-json-support-to-backend-api/03-01-SUMMARY.md` using the summary template.
</output>

