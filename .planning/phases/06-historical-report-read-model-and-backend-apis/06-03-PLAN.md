---
phase: 06-historical-report-read-model-and-backend-apis
plan: 03
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - be/lib/app/stocks/report_detail_handler.go
  - be/lib/app/stocks/report_detail_handler_test.go
  - be/cmd/server/main.go
autonomous: true
requirements:
  - APIH-03
must_haves:
  truths:
    - "GET /v1/stocks/{ticker}/reports/{reportId} resolves a single stock_reports row by ticker plus reportId"
    - "Historical detail locale behavior matches current latest-detail behavior: zh-TW detail key when available, otherwise EN detail key, otherwise 404"
    - "The historical detail endpoint downloads exactly one R2 JSON artifact per request"
    - "The endpoint does not guess R2 keys from reportId or path conventions"
    - "Route registration in cmd/server/main.go exposes the new handler without changing existing latest-detail routes"
  artifacts:
    - path: "be/lib/app/stocks/report_detail_handler.go"
      provides: "Historical detail handler for /v1/stocks/{ticker}/reports/{reportId}"
      contains: "/v1/stocks/{ticker}/reports/{reportId}"
    - path: "be/lib/app/stocks/report_detail_handler_test.go"
      provides: "Locale and not-found coverage for the historical detail route"
      contains: "locale=zh-TW"
    - path: "be/cmd/server/main.go"
      provides: "Router registration for the new historical detail handler"
      contains: "NewReportDetailHandler"
  key_links:
    - from: "be/lib/app/stocks/report_detail_handler.go"
      to: "be/turso/query/stocks.sql"
      via: "GetStockReportByTickerAndID"
      pattern: "GetStockReportByTickerAndID"
    - from: "be/lib/app/stocks/report_detail_handler.go"
      to: "be/lib/pkg/r2/client.go"
      via: "Download by persisted detail key"
      pattern: "Download"
---

<objective>
Add the locale-aware historical detail endpoint keyed by `reportId`.

Purpose: Complete the historical API surface so Phase 7 can load selected and compare revisions from live APIs without guessing keys or relying on summary-only list data.

Output: a new `/v1/stocks/{ticker}/reports/{reportId}` handler with route registration and tests for default locale, zh-TW preference/fallback, and not-found behavior.
</objective>

<execution_context>
@/Users/huangchihan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangchihan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-historical-report-read-model-and-backend-apis/06-RESEARCH.md
@.planning/phases/06-historical-report-read-model-and-backend-apis/06-VALIDATION.md
@.planning/phases/06-historical-report-read-model-and-backend-apis/06-01-PLAN.md
@be/lib/app/stocks/detail_handler.go
@be/lib/app/stocks/detail_handler_test.go
@be/cmd/server/main.go
@web/docs/historical-analysis-reports-prd.md

<repo_truth>
Current latest-detail behavior to mirror:

```go
key := row.R2DetailKey
if isZhTWLocale(r) && row.R2DetailZhTwKey != "" {
    key = row.R2DetailZhTwKey
}

if key == "" {
    render.ChiErr(w, r, http.StatusNotFound, errors.New("stock detail not found"))
    return
}
```

Phase 6 should reuse this exact locale rule for historical detail reads, but resolve the row via `ticker + reportId` from `stock_reports`.
</repo_truth>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a report-detail handler backed by GetStockReportByTickerAndID</name>
  <files>be/lib/app/stocks/report_detail_handler.go</files>
  <read_first>
    - be/lib/app/stocks/detail_handler.go (existing latest-detail handler pattern)
    - be/lib/app/stocks/locale.go (exact locale helper)
    - be/lib/app/turso_models/stocks.sql.go (generated GetStockReportByTickerAndID query after Plan 01)
    - be/lib/pkg/r2/client.go (Download API)
    - .planning/phases/06-historical-report-read-model-and-backend-apis/06-RESEARCH.md (historical detail locale rule)
  </read_first>
  <action>
Create a new file `be/lib/app/stocks/report_detail_handler.go`.

Implement a handler named `ReportDetailHandler` with constructor `NewReportDetailHandler`.

The handler must:
1. Register this exact route:
   ```go
   r.Get("/v1/stocks/{ticker}/reports/{reportId}", h.Handle)
   ```
2. Uppercase the `ticker` path param and read `reportId` from the route.
3. Call `GetStockReportByTickerAndID(r.Context(), turso_models.GetStockReportByTickerAndIDParams{Ticker: ticker, ID: reportID})`.
4. On `sql.ErrNoRows`, return `404` with `not found`.
5. Apply the exact locale rule:
   - start with `key := row.R2DetailKey`
   - if `isZhTWLocale(r)` and `row.R2DetailZhTwKey != ""`, replace `key` with `row.R2DetailZhTwKey`
6. If `key == ""`, return `404` with the exact message `stock report detail not found`.
7. Download the JSON via `r2Client.Download` and return it as `json.RawMessage`.

Do not guess the R2 key from `reportId`, `ticker`, or date. The endpoint must trust only the persisted DB columns.
  </action>
  <verify>
    <automated>cd /Users/huangchihan/develop/deep-value/be && go test ./lib/app/stocks -count=1</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "/v1/stocks/{ticker}/reports/{reportId}" be/lib/app/stocks/report_detail_handler.go returns 1 match
    - grep -n "GetStockReportByTickerAndID" be/lib/app/stocks/report_detail_handler.go returns at least 1 match
    - grep -n "stock report detail not found" be/lib/app/stocks/report_detail_handler.go returns 1 match
    - grep -n "isZhTWLocale" be/lib/app/stocks/report_detail_handler.go returns at least 1 match
    - grep -n "Download" be/lib/app/stocks/report_detail_handler.go returns at least 1 match
  </acceptance_criteria>
  <done>The codebase now has a dedicated historical detail handler that resolves one persisted report row and serves one locale-aware R2 detail artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Register the route and add regression tests for locale fallback and not-found behavior</name>
  <files>be/lib/app/stocks/report_detail_handler_test.go, be/cmd/server/main.go</files>
  <read_first>
    - be/lib/app/stocks/detail_handler_test.go (test structure and fake query/storage pattern)
    - be/cmd/server/main.go (current stock route registrations)
    - be/lib/app/stocks/report_detail_handler.go (new handler)
    - .planning/phases/06-historical-report-read-model-and-backend-apis/06-VALIDATION.md (required test coverage)
  </read_first>
  <action>
Create `be/lib/app/stocks/report_detail_handler_test.go` with a fake query and fake storage pattern matching the current `detail_handler_test.go` style.

Add tests covering these exact cases:
1. `locale=zh-TW` prefers `R2DetailZhTwKey`
2. `locale=zh-TW` falls back to `R2DetailKey` when `R2DetailZhTwKey` is empty
3. default locale uses `R2DetailKey`
4. handler returns `404` when both detail keys are empty
5. handler returns `404` when `GetStockReportByTickerAndID` returns `sql.ErrNoRows`

In each success case, assert the last downloaded key matches the expected persisted DB key.

Update `be/cmd/server/main.go` to register `router.AsRoute(stocks.NewReportDetailHandler)` alongside the existing stock handlers, without removing or reordering the latest-detail and reports-list registrations in a way that would shadow routes.

Finish by running the focused stocks tests and then the full backend suite.
  </action>
  <verify>
    <automated>cd /Users/huangchihan/develop/deep-value/be && go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "NewReportDetailHandler" be/cmd/server/main.go returns 1 match
    - grep -n "locale=zh-TW" be/lib/app/stocks/report_detail_handler_test.go returns at least 2 matches
    - grep -n "stock report detail not found" be/lib/app/stocks/report_detail_handler_test.go returns at least 1 match
    - grep -n "sql.ErrNoRows" be/lib/app/stocks/report_detail_handler_test.go returns at least 1 match
    - cd /Users/huangchihan/develop/deep-value/be && go test ./lib/app/stocks -count=1 exits 0
    - cd /Users/huangchihan/develop/deep-value/be && go test ./... exits 0
  </acceptance_criteria>
  <done>The historical detail route is registered, fully tested, and consistent with the existing latest-detail locale semantics.</done>
</task>

</tasks>

<verification>
- `cd /Users/huangchihan/develop/deep-value/be && go test ./lib/app/stocks -count=1` completes successfully
- `cd /Users/huangchihan/develop/deep-value/be && go test ./...` completes successfully
- `grep "NewReportDetailHandler" be/cmd/server/main.go` confirms the route is registered
</verification>

<success_criteria>
- `/v1/stocks/{ticker}/reports/{reportId}` exists and is route-registered
- the handler resolves report rows by persisted DB lookup, not key guessing
- zh-TW detail behavior matches the current latest-detail rule
- missing rows and missing detail keys return clear 404s
- focused and full Go test suites pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-historical-report-read-model-and-backend-apis/06-03-SUMMARY.md`
</output>
