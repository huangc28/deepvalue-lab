---
phase: 06-historical-report-read-model-and-backend-apis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - be/turso/migrations/*_stock_reports_historical_read_model.sql
  - be/turso/schema.sql
  - be/turso/query/stocks.sql
  - be/lib/app/turso_models/models.go
  - be/lib/app/turso_models/stocks.sql.go
  - be/lib/app/stocks/publish_handler.go
  - be/lib/app/stocks/publish_handler_test.go
autonomous: true
requirements:
  - APIH-01
must_haves:
  truths:
    - "Each historical report row persists EN detail key, optional zh-TW detail key, EN summary JSON, and optional zh-TW summary JSON"
    - "stock_reports becomes the per-report historical read model; published_stock_details remains the latest-only read model"
    - "InsertStockReport persists the historical summary/detail columns at publish time"
    - "ListStockReportsByTicker only returns rows with persisted summary JSON and EN detail key, preventing metadata-only historical rows from leaking into the stable API contract"
    - "GetStockReportByTickerAndID exists for historical detail lookup by ticker plus reportId"
    - "publish_handler tests assert both InsertStockReport and UpsertPublishedStockDetail keep working"
  artifacts:
    - path: "be/turso/query/stocks.sql"
      provides: "Historical read-model columns and queries"
      contains: "GetStockReportByTickerAndID"
    - path: "be/lib/app/stocks/publish_handler.go"
      provides: "Publish-time persistence of per-report summary/detail metadata"
      contains: "InsertStockReportParams"
    - path: "be/lib/app/stocks/publish_handler_test.go"
      provides: "Regression coverage for EN-only and bilingual publish requests"
      contains: "InsertStockReport"
  key_links:
    - from: "be/lib/app/stocks/publish_handler.go"
      to: "be/turso/query/stocks.sql"
      via: "InsertStockReportParams now includes detail-key and summary-json fields"
      pattern: "InsertStockReport"
    - from: "be/turso/query/stocks.sql"
      to: "be/lib/app/stocks/reports_list_handler.go"
      via: "ListStockReportsByTicker returns historical summary rows"
      pattern: "summary_json"
---

<objective>
Persist historical per-report summary data and detail locator keys at publish time.

Purpose: Build the storage contract that makes historical list reads performant and historical detail reads resolvable by `reportId` without request-time R2 fan-out or key guessing.

Output: a migration that extends `stock_reports`, updated sqlc queries/models, and a publish handler that writes historical summary/detail metadata into each report row while preserving the latest-only `published_stock_details` path.
</objective>

<execution_context>
@/Users/huangchihan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangchihan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-historical-report-read-model-and-backend-apis/06-RESEARCH.md
@.planning/phases/06-historical-report-read-model-and-backend-apis/06-VALIDATION.md
@docs/product/deepvalue-lab-analysis-publish-architecture.md
@web/docs/historical-analysis-reports-prd.md

<repo_truth>
The current repo already persists latest-only detail and summary data in `published_stock_details`, but `stock_reports` is still metadata-only:

```sql
CREATE TABLE stock_reports (
  id              TEXT PRIMARY KEY,
  ticker          TEXT NOT NULL,
  r2_key          TEXT NOT NULL,
  provenance      TEXT NOT NULL DEFAULT '',
  published_at_ms INTEGER NOT NULL DEFAULT (unixepoch('now') * 1000),
  created_at_ms   INTEGER NOT NULL DEFAULT (unixepoch('now') * 1000)
);
```

Current `InsertStockReport` also only persists markdown metadata:

```sql
INSERT INTO stock_reports (id, ticker, r2_key, provenance, published_at_ms)
VALUES (?, ?, ?, ?, ?);
```
</repo_truth>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend stock_reports into the historical read model and regenerate sqlc</name>
  <files>be/turso/migrations/*_stock_reports_historical_read_model.sql, be/turso/schema.sql, be/turso/query/stocks.sql, be/lib/app/turso_models/models.go, be/lib/app/turso_models/stocks.sql.go</files>
  <read_first>
    - be/turso/schema.sql (current stock_reports table shape)
    - be/turso/query/stocks.sql (current InsertStockReport and ListStockReportsByTicker queries)
    - be/turso/migrations/20260318065637_stock_domain.sql (migration style)
    - be/turso/migrations/20260320220959_published_stock_details_zh_tw_locale.sql (pattern for locale-aware summary/detail columns)
    - be/sqlc.yaml (sqlc generation config)
    - .planning/phases/06-historical-report-read-model-and-backend-apis/06-RESEARCH.md (recommended storage design and legacy-row handling)
  </read_first>
  <action>
Create a new Goose migration named with the exact suffix `stock_reports_historical_read_model.sql` under `be/turso/migrations/`.

In the migration `Up` section:
1. Add these columns to `stock_reports`:
   - `r2_detail_key TEXT NOT NULL DEFAULT ''`
   - `r2_detail_zh_tw_key TEXT NOT NULL DEFAULT ''`
   - `summary_json TEXT NOT NULL DEFAULT '{}' CHECK (json_valid(summary_json))`
   - `summary_json_zh_tw TEXT NOT NULL DEFAULT '{}' CHECK (json_valid(summary_json_zh_tw))`
2. Backfill the current latest row by copying data from `published_stock_details` where `stock_reports.id = published_stock_details.report_id`:
   - `r2_detail_key <- published_stock_details.r2_detail_key`
   - `r2_detail_zh_tw_key <- published_stock_details.r2_detail_zh_tw_key`
   - `summary_json <- published_stock_details.summary_json`
   - `summary_json_zh_tw <- published_stock_details.summary_json_zh_tw`

In the migration `Down` section:
1. Rebuild `stock_reports` back to its original metadata-only shape.
2. Preserve existing columns `id`, `ticker`, `r2_key`, `provenance`, `published_at_ms`, and `created_at_ms`.

Update `be/turso/schema.sql` so the checked-in schema matches the migrated table exactly.

Update `be/turso/query/stocks.sql`:
1. Expand `InsertStockReport` to insert these exact columns in this order:
   - `id`
   - `ticker`
   - `r2_key`
   - `r2_detail_key`
   - `r2_detail_zh_tw_key`
   - `summary_json`
   - `summary_json_zh_tw`
   - `provenance`
   - `published_at_ms`
2. Change `ListStockReportsByTicker` to:
   - return rows ordered by `published_at_ms DESC`
   - filter out rows where `summary_json = '{}'`
   - filter out rows where `r2_detail_key = ''`
3. Add `GetStockReportByTickerAndID :one` with:
   ```sql
   SELECT * FROM stock_reports
   WHERE ticker = ? AND id = ?
   LIMIT 1;
   ```

Run `cd /Users/huangchihan/develop/deep-value/be && make gen/sqlc` so `be/lib/app/turso_models/models.go` and `be/lib/app/turso_models/stocks.sql.go` regenerate with the new query/column surface.
  </action>
  <verify>
    <automated>cd /Users/huangchihan/develop/deep-value/be && make gen/sqlc && go test ./lib/app/stocks -count=1</automated>
  </verify>
  <acceptance_criteria>
    - ls be/turso/migrations/*_stock_reports_historical_read_model.sql returns exactly 1 path
    - grep "r2_detail_key" be/turso/schema.sql returns a match inside the stock_reports definition
    - grep "summary_json_zh_tw" be/turso/schema.sql returns a match inside the stock_reports definition
    - grep -n "INSERT INTO stock_reports" be/turso/query/stocks.sql returns a match with r2_detail_key and summary_json columns included
    - grep -n "GetStockReportByTickerAndID" be/turso/query/stocks.sql returns exactly 1 match
    - grep -n "summary_json != '{}'" be/turso/query/stocks.sql returns 1 match
    - grep -n "r2_detail_key != ''" be/turso/query/stocks.sql returns 1 match
    - grep -n "R2DetailKey" be/lib/app/turso_models/models.go returns at least 1 match for the StockReport model or generated params
    - cd /Users/huangchihan/develop/deep-value/be && make gen/sqlc exits 0
  </acceptance_criteria>
  <done>The database and generated query layer now support a per-report historical read model with summary/detail metadata persisted on every stock_reports row.</done>
</task>

<task type="auto">
  <name>Task 2: Persist historical summary/detail columns in the publish handler and lock regressions with tests</name>
  <files>be/lib/app/stocks/publish_handler.go, be/lib/app/stocks/publish_handler_test.go</files>
  <read_first>
    - be/lib/app/stocks/publish_handler.go (current report upload, summary extraction, InsertStockReport call, latest-only upsert)
    - be/lib/app/stocks/publish_handler_test.go (current EN-only and bilingual coverage)
    - be/lib/app/turso_models/stocks.sql.go (generated InsertStockReportParams after Task 1)
    - .planning/phases/06-historical-report-read-model-and-backend-apis/06-RESEARCH.md (publish-time persistence rule)
  </read_first>
  <action>
Update `be/lib/app/stocks/publish_handler.go` so the `InsertStockReport` call writes the new per-report historical columns alongside the existing markdown metadata.

The exact `InsertStockReportParams` values must be:
- `ID: reportID`
- `Ticker: ticker`
- `R2Key: r2ReportKey`
- `R2DetailKey: r2DetailKey`
- `R2DetailZhTwKey: r2DetailZhTWKey`
- `SummaryJson: summaryJSON`
- `SummaryJsonZhTw: summaryJSONZhTW`
- `Provenance: req.Report.Provenance`
- `PublishedAtMs: publishedAtMs`

Preserve the existing `UpsertPublishedStockDetail` call exactly as the latest-detail write path. This phase adds historical read-model persistence; it must not replace the latest-only read model.

Update `be/lib/app/stocks/publish_handler_test.go`:
1. Extend `fakePublishQueries` so `insertStockReportCalls` captures the generated `InsertStockReportParams`.
2. In `TestPublishHandler_ENOnlyKeepsZhTWDefaults`, assert:
   - exactly one `InsertStockReport` call occurred
   - `R2DetailKey` is non-empty
   - `R2DetailZhTwKey` is empty
   - `SummaryJson` is not `'{}'`
   - `SummaryJsonZhTw` is `'{}'`
3. In `TestPublishHandler_BilingualRequestPersistsZhTWArtifact`, assert:
   - exactly one `InsertStockReport` call occurred
   - both `R2DetailKey` and `R2DetailZhTwKey` are non-empty
   - both `SummaryJson` and `SummaryJsonZhTw` are not `'{}'`
4. Keep the invalid zh-TW request test proving no persistence occurs on a `422`.
  </action>
  <verify>
    <automated>cd /Users/huangchihan/develop/deep-value/be && go test ./lib/app/stocks -count=1</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "InsertStockReport(r.Context()" be/lib/app/stocks/publish_handler.go returns 1 match
    - grep -n "R2DetailKey:" be/lib/app/stocks/publish_handler.go returns a match inside InsertStockReportParams
    - grep -n "SummaryJson:" be/lib/app/stocks/publish_handler.go returns a match inside InsertStockReportParams
    - grep -n "SummaryJsonZhTw:" be/lib/app/stocks/publish_handler.go returns a match inside InsertStockReportParams
    - grep -n "insertStockReportCalls" be/lib/app/stocks/publish_handler_test.go returns at least 2 matches
    - grep -n "SummaryJsonZhTw" be/lib/app/stocks/publish_handler_test.go returns at least 2 assertion matches
    - cd /Users/huangchihan/develop/deep-value/be && go test ./lib/app/stocks -count=1 exits 0
  </acceptance_criteria>
  <done>Every publish now stores per-report historical summary/detail metadata in stock_reports, and the publish handler tests lock both EN-only and bilingual behavior.</done>
</task>

</tasks>

<verification>
- `cd /Users/huangchihan/develop/deep-value/be && make gen/sqlc` completes successfully
- `cd /Users/huangchihan/develop/deep-value/be && go test ./lib/app/stocks -count=1` completes successfully
- `grep "GetStockReportByTickerAndID" be/turso/query/stocks.sql` confirms historical detail lookup query exists
</verification>

<success_criteria>
- stock_reports stores per-report summary JSON and detail keys
- publish_handler writes those values on every publish
- latest-only published_stock_details flow remains intact
- sqlc models regenerate successfully
- focused stocks handler tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-historical-report-read-model-and-backend-apis/06-01-SUMMARY.md`
</output>
