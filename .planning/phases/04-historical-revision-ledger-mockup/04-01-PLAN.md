---
phase: 04-historical-revision-ledger-mockup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/pages/stock-detail-page.tsx
  - web/src/types/stocks.ts
  - web/src/data/mock-stocks.ts
  - web/src/components/detail/*
  - web/src/i18n/messages.ts
  - web/src/index.css
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
  - HIST-04
  - HIST-05

must_haves:
  truths:
    - "The old text-only History block is replaced by a revision-ledger mockup when structured historical mock data exists"
    - "Revision rows are sorted by `publishedAtMs` descending and the latest revision auto-opens by default"
    - "The mockup explicitly handles no-history, single-revision, and many-revision states"
    - "Compare mode supports exactly two revisions and preserves the base selection when compare exits or one side is cleared"
    - "The mockup remains inside the existing stock detail page and uses local mock data only"
    - "Keyboard interaction is supported for revision selection and compare flow"
  artifacts:
    - path: "web/src/pages/stock-detail-page.tsx"
      provides: "Historical revision ledger mockup integrated into the stock detail page"
      contains: "latest-first revision list, selected snapshot, compare state"
    - path: "web/src/data/mock-stocks.ts"
      provides: "Historical mock data for empty, single, and many-revision cases"
      contains: "at least one stock with 4+ revisions"
    - path: "web/src/types/stocks.ts"
      provides: "Temporary structured mock history shape for Phase 4"
      contains: "publishedAtMs"
  key_links:
    - from: "web/src/data/mock-stocks.ts"
      to: "web/src/pages/stock-detail-page.tsx"
      via: "selected stock page can render revision list, snapshot, and compare state from local historical mock data"
      pattern: "publishedAtMs|provenance|latest"
    - from: "web/src/pages/stock-detail-page.tsx"
      to: "web/src/i18n/messages.ts"
      via: "new revision-ledger UI chrome stays localized"
      pattern: "history|revision|compare|latest"
---

<objective>
Build a frontend-only historical revision ledger mockup inside the stock detail page so the user can manually review and tune the visual direction before later phases lock the interaction contract, data model, and backend API shape.

Purpose: the current History section is only a text list and cannot support revision review or compare flows. This phase makes the feature tangible without introducing live data or backend dependencies.
Output: local historical mock data, a revision-ledger UI integrated into the stock detail page, explicit empty/single/many states, compare mode, and a supporting trend visualization.
</objective>

<execution_context>
@/Users/huangchihan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangchihan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-historical-revision-ledger-mockup/04-CONTEXT.md
@.planning/phases/04-historical-revision-ledger-mockup/04-VALIDATION.md

<interfaces>
This phase may introduce a temporary frontend-only structured history mock shape, but it must not finalize the production API contract.

Temporary mock fields must be sufficient to render:

- `reportId`
- `publishedAtMs`
- `provenance`
- `valuationStatus`
- `thesisStatus`
- `technicalEntryStatus`
- `currentPrice`
- `bearFairValue`
- `baseFairValue`
- `bullFairValue`
- `summary`
- `currentPriceImpliesBrief`
- `currentPriceImplies`
- `monitorNext`
- `latest`

The mockup may keep the existing `history` field for compatibility, but the historical revision ledger should render from the structured mock history data when present.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce temporary structured historical mock data and seed review states</name>
  <files>web/src/types/stocks.ts, web/src/data/mock-stocks.ts</files>

  <read_first>
    - `web/src/types/stocks.ts` — current `StockDetail` shape, especially `history`
    - `web/src/data/mock-stocks.ts` — current mock stock data and existing stock coverage
    - `web/docs/historical-analysis-reports-prd.md` — milestone behavior for latest-first, compare mode, and stop-after-mockup intent
  </read_first>

  <action>
Add a temporary frontend-only structured historical mock shape to support the Phase 4 revision ledger.

Seed local mock data so that:

- one stock has 4 or more meaningful historical revisions
- one stock exercises a single-revision state
- one stock exercises an empty-history state

Each seeded revision must carry enough information to render the list row, selected snapshot, compare view, and trend visualization without live API data.

Do not finalize backend-oriented fields such as locale metadata in this phase unless they are needed for simple placeholder rendering.
  </action>

  <verify>
    <automated>cd /Users/huangchihan/develop/profound-stock/web && pnpm lint && pnpm build</automated>
  </verify>

  <acceptance_criteria>
    - `web/src/types/stocks.ts` contains a temporary structured historical mock shape with `publishedAtMs`
    - `web/src/data/mock-stocks.ts` contains at least one stock with 4+ historical revisions
    - the data set includes explicit empty-history and single-revision cases
    - `cd /Users/huangchihan/develop/profound-stock/web && pnpm lint && pnpm build` exits 0
  </acceptance_criteria>

  <done>Local mock data can drive all key historical-reports UI states without any backend dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the History section with a revision-ledger mockup and selected snapshot</name>
  <files>web/src/pages/stock-detail-page.tsx, web/src/components/detail/*, web/src/i18n/messages.ts, web/src/index.css</files>

  <read_first>
    - `web/src/pages/stock-detail-page.tsx` — current history section and surrounding layout
    - `web/src/components/ui/panel.tsx` — section shell
    - `web/src/components/ui/status-badge.tsx` — badge language
    - `web/src/index.css` — current tokens and typography
    - `web/src/i18n/messages.ts` — localized stock detail chrome strings
  </read_first>

  <action>
Replace the current text-only History content with a revision-ledger mockup that includes:

- latest-first revision list
- selected revision snapshot
- explicit no-history and single-revision states
- latest marker, selected marker, and compare-ready affordance
- concise "what changed since previous revision" summary in the selected snapshot

Keep the feature inside the current stock detail page section hierarchy. Do not add a new route.

Use the existing panel/badge language so the mockup feels native to the current app rather than a separate prototype.
  </action>

  <verify>
    <automated>cd /Users/huangchihan/develop/profound-stock/web && pnpm lint && pnpm build</automated>
  </verify>

  <acceptance_criteria>
    - the stock detail page no longer renders historical revisions as a plain bullet list when structured mock data exists
    - latest revision auto-opens by default
    - no-history and single-revision states are explicitly rendered
    - new revision-ledger UI strings are localized through `web/src/i18n/messages.ts`
    - `cd /Users/huangchihan/develop/profound-stock/web && pnpm lint && pnpm build` exits 0
  </acceptance_criteria>

  <done>The stock detail page presents a reviewable historical revision ledger mockup instead of a text-only history block.</done>
</task>

<task type="auto">
  <name>Task 3: Add compare mode, supporting trend view, and responsive keyboard-ready behavior</name>
  <files>web/src/pages/stock-detail-page.tsx, web/src/components/detail/*, web/src/index.css</files>

  <read_first>
    - `web/src/pages/stock-detail-page.tsx` — selected snapshot logic from Task 2
    - `web/docs/historical-analysis-reports-prd.md` — compare mode and trend-view expectations
    - `web/src/components/detail/expectation-bridge.tsx` — reference for compact analytical visualization
  </read_first>

  <action>
Add compare mode and a supporting trend visualization to the revision ledger mockup.

Rules to implement:

- compare mode is exactly two revisions
- base selection is preserved when compare exits
- clearing one side of compare does not clear the base selection
- mobile compare stacks vertically
- keyboard focus and selection states are visible and usable
- trend view supports the revision story but does not become the dominant visual element

The trend view may be implemented as a compact custom rail or lightweight visualization rather than a production chart library.
  </action>

  <verify>
    <automated>cd /Users/huangchihan/develop/profound-stock/web && pnpm lint && pnpm build</automated>
  </verify>

  <acceptance_criteria>
    - compare mode supports selecting, clearing, and exiting exactly two revisions
    - the base selected revision is preserved across compare transitions
    - mobile compare layout stacks without horizontal overflow
    - focus states are visible for keyboard interaction
    - the trend view helps distinguish current price movement from fair value revision at a glance
    - `cd /Users/huangchihan/develop/profound-stock/web && pnpm lint && pnpm build` exits 0
  </acceptance_criteria>

  <done>The mockup supports reviewable compare and trend behaviors and is ready for manual visual tuning.</done>
</task>

</tasks>

<verification>
Run these checks after execution:

1. `cd web && pnpm lint`
2. `cd web && pnpm build`
3. Open the stock detail page for the seeded many-revision stock and confirm latest auto-opens
4. Open a single-revision stock and confirm compare affordances are inactive
5. Open an empty-history stock and confirm explicit empty state
6. On desktop and mobile widths, confirm compare mode can be entered and exited without layout breakage
</verification>

<success_criteria>
- the stock detail page renders a historical revision ledger mockup from local mock data
- latest revision is auto-selected and clearly marked
- compare mode supports exactly two revisions and survives enter/exit/clear flows without losing the base selection
- explicit empty/single/many revision states are all present
- the UI is ready for manual review and tuning before later phases begin
</success_criteria>

<output>
After completion, create `.planning/phases/04-historical-revision-ledger-mockup/04-01-SUMMARY.md` using the summary template.
</output>
