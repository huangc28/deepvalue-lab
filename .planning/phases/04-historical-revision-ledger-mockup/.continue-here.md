---
phase: 04-historical-revision-ledger-mockup
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-03-24T09:31:00.030Z
---

<current_state>
Phase 4 UI is partially implemented. Tasks 1 and 2 are done (types, mock data, revision ledger component all exist). Task 3 (compare mode + trend view) is NOT implemented — confirmed by the UI audit (18/24) run this session. The component file is `web/src/components/detail/historical-revision-ledger.tsx`.

A UI audit was just completed and committed (26c0494). The top 3 findings are the immediate next actions.
</current_state>

<completed_work>

- Task 1: Structured historical mock data + types — Done (`web/src/types/stocks.ts`, `web/src/data/mock-stocks.ts`)
- Task 2: Revision ledger UI + selected snapshot — Done (`web/src/components/detail/historical-revision-ledger.tsx`)
- UI Audit (gsd:ui-review): Done — 18/24 score, report at `.planning/phases/04-historical-revision-ledger-mockup/04-UI-REVIEW.md`
</completed_work>

<remaining_work>

- Task 3 (NOT started): Add compare mode, trend view, responsive + keyboard behavior
  - Compare mode: exactly 2 revisions, preserve base selection on exit/clear
  - Mobile compare stacks vertically
  - Keyboard focus states visible
  - Trend view: compact, supporting layer only, must NOT dominate

- Priority fixes from UI audit (fix these as part of Task 3 or before):
  1. Compare mode absent — 12 i18n keys in messages.ts exist but unreachable, no compare state/buttons/card in component
  2. Trend date grid hardcoded `grid-cols-4` at line 352 — fix with dynamic `gridTemplateColumns` from `chronological.length`
  3. Empty-state copy has dev placeholder: "mocked" in `historyEmptyTitle` — user-facing text, fix the copy

- No SUMMARY.md written yet — should be created after Task 3 completes
</remaining_work>

<decisions_made>

- Azure Container Apps replicas changed to min:1 max:1 for both web and be (deployed live, ~$72/month cost)
- Obsidian = personal knowledge (human reads), NotebookLM = project knowledge (AI agents query) — documented in vault
- Global CLAUDE.md and AGENTS.md updated to load `AI Note-Taking Principles.md` before writing vault notes
- Phase 4 CONTEXT.md decisions remain valid — dark mono-first, compare mode exactly 2 revisions, trend is supporting layer only
</decisions_made>

<blockers>

- None blocking. Task 3 can start immediately.
</blockers>

<context>
The component already renders a working revision ledger. The visual direction (dark mono-first, badge language, panel style) scored well (Color 4/4). The main gap is behavioral — compare mode was specified in CONTEXT.md and the plan but never implemented. The 12 i18n keys in messages.ts are already there waiting to be wired up.

Key constraint: do NOT make the trend view visually dominant — DeepValue's principle is "judgment before raw data". The trend is a supporting annotation, not the hero element.
</context>

<next_action>
Start with: Fix the 3 priority items from the UI audit (especially compare mode — Task 3), then write SUMMARY.md, then run `/gsd:verify-work 4` for UAT, then `/gsd:plan-phase 5`.

Relevant files:
- Component: `web/src/components/detail/historical-revision-ledger.tsx`
- i18n: `web/src/i18n/messages.ts` (compare keys already exist)
- Audit report: `.planning/phases/04-historical-revision-ledger-mockup/04-UI-REVIEW.md`
- Plan: `.planning/phases/04-historical-revision-ledger-mockup/04-01-PLAN.md`
- Context: `.planning/phases/04-historical-revision-ledger-mockup/04-CONTEXT.md`
</next_action>
