---
phase: 07-frontend-api-integration-for-historical-reports
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - web/src/lib/api.ts
  - web/src/lib/queries.ts
  - web/src/pages/stock-detail-page.tsx
autonomous: true
requirements:
  - INTH-01
  - INTH-02
must_haves:
  truths:
    - "A live stock detail page reads the revision list from /v1/stocks/{ticker}/reports instead of mixing live latest detail with mock history"
    - "Selected and compare historical detail payloads load lazily per reportId and reuse React Query cache entries by reportId"
    - "Mock historical data is used only when the entire stock detail page is already in mock fallback because the latest detail request is unavailable"
    - "Merged selected and compare cards keep localeHasFallback, latest, and provenance from the summary row while showing detail-only fields from the detail response"
    - "History API failures degrade inside the History section while the current latest detail path keeps rendering normally"
  artifacts:
    - path: "web/src/lib/api.ts"
      provides: "Historical report list and detail client functions"
      contains: "fetchStockReports"
    - path: "web/src/lib/queries.ts"
      provides: "Historical report query hooks and summary-plus-detail merge helper"
      contains: "mergeHistoricalReportDetail"
    - path: "web/src/pages/stock-detail-page.tsx"
      provides: "Live history orchestration with explicit mock boundary and section-level degradation"
      contains: "useMockHistory"
  key_links:
    - from: "web/src/pages/stock-detail-page.tsx"
      to: "web/src/lib/queries.ts"
      via: "useStockReports and useStockReportDetail"
      pattern: "useStockReports"
    - from: "web/src/lib/queries.ts"
      to: "web/src/lib/api.ts"
      via: "historical list and detail fetchers"
      pattern: "fetchStockReportDetail"
    - from: "web/src/pages/stock-detail-page.tsx"
      to: "web/src/components/detail/historical-revision-ledger.tsx"
      via: "mode, selectedId, compareId, and async state props"
      pattern: "mode={useMockHistory ? 'legacy' : 'live'}"
---

<objective>
Wire the stock detail page to the live historical report APIs without regressing the current latest-detail experience.

Purpose: Complete the Phase 7 integration by separating latest-detail reads from historical reads, using lazy per-report detail queries, and confining historical failures to the History section.

Output: live historical report client functions, React Query hooks keyed by ticker/reportId/locale, and a stock detail page that drives the ledger with live summary/detail data or full-page mock fallback only.
</objective>

<execution_context>
@/Users/huangchihan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangchihan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-frontend-api-integration-for-historical-reports/07-CONTEXT.md
@.planning/phases/07-frontend-api-integration-for-historical-reports/07-RESEARCH.md
@.planning/phases/07-frontend-api-integration-for-historical-reports/07-01-PLAN.md
@.planning/phases/06-historical-report-read-model-and-backend-apis/06-02-SUMMARY.md
@.planning/phases/06-historical-report-read-model-and-backend-apis/06-03-SUMMARY.md
@web/docs/historical-analysis-reports-prd.md
@web/src/lib/api.ts
@web/src/lib/queries.ts
@web/src/pages/stock-detail-page.tsx
@web/src/components/detail/historical-revision-ledger.tsx
@web/src/types/stocks.ts
@be/lib/app/stocks/reports_list_handler.go
@be/lib/app/stocks/history_summary_contract.go
@be/lib/app/stocks/report_detail_handler.go

<interfaces>
Current latest-detail query contract from `web/src/lib/queries.ts`:

```ts
export function useStock(ticker: string, locale: Locale) {
  return useQuery({
    queryKey: ['stocks', ticker, locale],
    queryFn: () => fetchStock(ticker, locale),
    staleTime: 5 * 60 * 1000,
  })
}
```

Historical API contracts from Phase 6:

```go
r.Get("/v1/stocks/{ticker}/reports", h.Handle)
r.Get("/v1/stocks/{ticker}/reports/{reportId}", h.Handle)
```

Required frontend merge target from `web/src/types/stocks.ts`:

```ts
export interface HistoricalReportDetail extends HistoricalReportSummary {
  currentPriceImpliesBrief: LocalizedText
  currentPriceImplies: LocalizedText
  changeSummary: LocalizedText
  monitorNext: LocalizedText[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add live historical report client functions, query hooks, and summary-detail merge logic</name>
  <files>web/src/lib/api.ts, web/src/lib/queries.ts</files>
  <read_first>
    - web/src/lib/api.ts
    - web/src/lib/queries.ts
    - web/src/types/stocks.ts
    - be/lib/app/stocks/reports_list_handler.go
    - be/lib/app/stocks/history_summary_contract.go
    - be/lib/app/stocks/report_detail_handler.go
    - .planning/phases/07-frontend-api-integration-for-historical-reports/07-CONTEXT.md
  </read_first>
  <action>
Update `web/src/lib/api.ts` with these exact helpers and exports:

1. `buildStockReportsUrl(ticker: string, locale: Locale)`
   - returns `${API_URL}/v1/stocks/${encodedTicker}/reports?locale=zh-TW` when locale is `zh-TW`
   - otherwise returns `${API_URL}/v1/stocks/${encodedTicker}/reports`
2. `buildStockReportDetailUrl(ticker: string, reportId: string, locale: Locale)`
   - returns `${API_URL}/v1/stocks/${encodedTicker}/reports/${encodedReportId}?locale=zh-TW` when locale is `zh-TW`
   - otherwise returns `${API_URL}/v1/stocks/${encodedTicker}/reports/${encodedReportId}`
3. `fetchStockReports(ticker: string, locale: Locale): Promise<HistoricalReportSummary[]>`
   - calls `fetchJson<{ reports: HistoricalReportSummary[] }>(...)`
   - returns `data.reports`
4. `fetchStockReportDetail(ticker: string, reportId: string, locale: Locale): Promise<StockDetail>`
   - calls `fetchJson<StockDetail>(...)`

Update `web/src/lib/queries.ts` with these exact exports:

1. `useStockReports(ticker: string, locale: Locale, enabled = true)`
   - query key: `['stock-reports', ticker, locale]`
   - query fn: `() => fetchStockReports(ticker, locale)`
   - `enabled`
   - `staleTime: 5 * 60 * 1000`
2. `useStockReportDetail(ticker: string, reportId: string | null, locale: Locale, enabled = true)`
   - query key: `['stock-report-detail', ticker, reportId, locale]`
   - query fn: `() => fetchStockReportDetail(ticker, reportId!, locale)`
   - `enabled: enabled && Boolean(reportId)`
   - `staleTime: 5 * 60 * 1000`
3. `mergeHistoricalReportDetail(summary: HistoricalReportSummary, detail: StockDetail): HistoricalReportDetail`
   - must preserve all summary-row metadata with `{ ...summary, ... }`
   - set `currentPriceImpliesBrief` to `detail.currentPriceImpliesBrief ?? detail.currentPriceImplies`
   - set `currentPriceImplies` to `detail.currentPriceImplies`
   - set `changeSummary` to `detail.provisionalConclusion ?? detail.summary`
   - set `monitorNext` to `detail.monitorNext`

Do not change the existing `useStock(...)` query signature or its query key. Phase 7 augments the History section; it does not replace the latest-detail path per D-03.
  </action>
  <verify>
    <automated>cd /Users/huangchihan/develop/deep-value/web && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "function buildStockReportsUrl" web/src/lib/api.ts returns 1 match
    - grep -n "function buildStockReportDetailUrl" web/src/lib/api.ts returns 1 match
    - grep -n "export async function fetchStockReports" web/src/lib/api.ts returns 1 match
    - grep -n "export async function fetchStockReportDetail" web/src/lib/api.ts returns 1 match
    - grep -n "queryKey: \\['stock-reports', ticker, locale\\]" web/src/lib/queries.ts returns 1 match
    - grep -n "queryKey: \\['stock-report-detail', ticker, reportId, locale\\]" web/src/lib/queries.ts returns 1 match
    - grep -n "export function mergeHistoricalReportDetail" web/src/lib/queries.ts returns 1 match
    - grep -n "currentPriceImpliesBrief: detail.currentPriceImpliesBrief ?? detail.currentPriceImplies" web/src/lib/queries.ts returns 1 match
    - grep -n "changeSummary: detail.provisionalConclusion ?? detail.summary" web/src/lib/queries.ts returns 1 match
    - cd /Users/huangchihan/develop/deep-value/web && pnpm lint && pnpm build exits 0
  </acceptance_criteria>
  <done>The frontend now has stable live-history list/detail client functions, cacheable query hooks, and a single merge helper that preserves summary metadata on historical detail cards.</done>
</task>

<task type="auto">
  <name>Task 2: Drive the History section from live queries while preserving the current latest-detail boundary</name>
  <files>web/src/pages/stock-detail-page.tsx</files>
  <read_first>
    - web/src/pages/stock-detail-page.tsx
    - web/src/lib/queries.ts
    - web/src/components/detail/historical-revision-ledger.tsx
    - web/src/data/mock-stocks.ts
    - .planning/phases/07-frontend-api-integration-for-historical-reports/07-CONTEXT.md
    - web/docs/historical-analysis-reports-prd.md
  </read_first>
  <action>
Refactor `web/src/pages/stock-detail-page.tsx` to enforce the live/mock boundary from D-01 through D-12.

Keep these latest-detail lines intact in principle:
- `const { data: liveStock, isLoading, error } = useStock(ticker, locale)`
- page-level `LoadingState`, `404`, and `ErrorState` remain driven only by the latest stock-detail query

Add this exact history boundary:
```ts
const useMockHistory = !liveStock && !!mockStock
```

Replace the current mixed fallback expressions:
- `stock.historicalReports ?? mockStock?.historicalReports`
- `stock.historicalReportDetails ?? mockStock?.historicalReportDetails`

with this behavior:
1. When `useMockHistory` is true, keep the old mock history path:
   - `reports={mockStock?.historicalReports}`
   - `reportDetails={mockStock?.historicalReportDetails}`
   - `mode="legacy"`
2. When `liveStock` exists, drive history from live queries only:
   - `const reportsQuery = useStockReports(ticker, locale, Boolean(liveStock))`
   - page-level `selectedId` and `compareId` state using `useState<string | null>(null)`
   - resolve `resolvedSelectedId` from the first report when the current `selectedId` is missing
   - resolve `resolvedCompareId` only when it exists in the list and differs from `resolvedSelectedId`
   - `const selectedDetailQuery = useStockReportDetail(ticker, resolvedSelectedId, locale, Boolean(liveStock && resolvedSelectedId))`
   - `const compareDetailQuery = useStockReportDetail(ticker, resolvedCompareId, locale, Boolean(liveStock && resolvedCompareId))`
3. Build a live detail map with only the successfully fetched selected/compare rows:
   - merge each successful detail payload onto its matching summary row with `mergeHistoricalReportDetail(...)`
   - key the map by `reportId`
4. Pass the async-aware ledger props from Plan 01:
   - `mode={useMockHistory ? 'legacy' : 'live'}`
   - `selectedId={useMockHistory ? undefined : selectedId}`
   - `compareId={useMockHistory ? undefined : compareId}`
   - `onSelectedIdChange={useMockHistory ? undefined : setSelectedId}`
   - `onCompareIdChange={useMockHistory ? undefined : setCompareId}`
   - `listState={{ status: reportsQuery.isLoading ? 'loading' : reportsQuery.isError ? 'error' : 'ready', errorMessage: reportsQuery.error instanceof Error ? reportsQuery.error.message : undefined }}`
   - `selectedDetailState={{ status: !resolvedSelectedId ? 'idle' : selectedDetailQuery.isLoading ? 'loading' : selectedDetailQuery.isError ? 'error' : 'ready', errorMessage: selectedDetailQuery.error instanceof Error ? selectedDetailQuery.error.message : undefined }}`
   - `compareDetailState={{ status: !resolvedCompareId ? 'idle' : compareDetailQuery.isLoading ? 'loading' : compareDetailQuery.isError ? 'error' : 'ready', errorMessage: compareDetailQuery.error instanceof Error ? compareDetailQuery.error.message : undefined }}`
5. Keep `historyItems` only for the legacy/mock path:
   - `const historyItems = useMockHistory ? (stock.history ?? mockStock?.history ?? []) : []`

Do not collapse the whole page on `/reports` or `/reports/{reportId}` failure. The live hero, valuation, thesis, and latest-detail sections must continue to render as long as `useStock(...)` succeeded.
  </action>
  <verify>
    <automated>cd /Users/huangchihan/develop/deep-value/web && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "const useMockHistory = !liveStock && !!mockStock" web/src/pages/stock-detail-page.tsx returns 1 match
    - grep -n "const reportsQuery = useStockReports(ticker, locale, Boolean(liveStock))" web/src/pages/stock-detail-page.tsx returns 1 match
    - grep -n "const selectedDetailQuery = useStockReportDetail(ticker, resolvedSelectedId, locale, Boolean(liveStock && resolvedSelectedId))" web/src/pages/stock-detail-page.tsx returns 1 match
    - grep -n "const compareDetailQuery = useStockReportDetail(ticker, resolvedCompareId, locale, Boolean(liveStock && resolvedCompareId))" web/src/pages/stock-detail-page.tsx returns 1 match
    - grep -n "mode={useMockHistory ? 'legacy' : 'live'}" web/src/pages/stock-detail-page.tsx returns 1 match
    - rg -n "stock\\.historicalReports \\?\\? mockStock\\?\\.historicalReports|stock\\.historicalReportDetails \\?\\? mockStock\\?\\.historicalReportDetails" web/src/pages/stock-detail-page.tsx returns 0 matches
    - grep -n "mergeHistoricalReportDetail" web/src/pages/stock-detail-page.tsx returns at least 1 match
    - grep -n "listState={{ status: reportsQuery.isLoading ? 'loading' : reportsQuery.isError ? 'error' : 'ready'" web/src/pages/stock-detail-page.tsx returns 1 match
    - cd /Users/huangchihan/develop/deep-value/web && pnpm lint && pnpm build exits 0
  </acceptance_criteria>
  <done>The stock detail page now uses live historical list/detail APIs when latest detail is live, keeps mock history only for full mock fallback, and isolates all history failures inside the History section.</done>
</task>

</tasks>

<verification>
- `cd /Users/huangchihan/develop/deep-value/web && pnpm lint && pnpm build` completes successfully
- `rg "fetchStockReports|fetchStockReportDetail|mergeHistoricalReportDetail" web/src/lib` confirms the live history data layer exists
- `rg "useMockHistory|mode=\\{useMockHistory \\? 'legacy' : 'live'\\}" web/src/pages/stock-detail-page.tsx` confirms the live/mock boundary is explicit
</verification>

<success_criteria>
- Live pages read revision summaries from `/reports`
- Selected and compare detail reads are lazy and keyed by `reportId`
- Summary metadata survives detail fetches through frontend merging
- Mock history is no longer mixed into live latest-detail pages
- History API failures stay local to the History section
</success_criteria>

<output>
After completion, create `.planning/phases/07-frontend-api-integration-for-historical-reports/07-02-SUMMARY.md`
</output>
